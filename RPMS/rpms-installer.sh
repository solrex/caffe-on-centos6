#/bin/bash

# gflags
rpm -ivh gflags-*

# glog
rpm -ivh glog-*

# protobuf
rpm -ivh protobuf-*

# leveldb
rpm -ivh snappy-*
rpm -ivh leveldb-*

# atlas
rpm -ivh atlas-*

# cmake
rpm -ivh libarchive-*
rpm -ivh cmake*

# hdf5
rpm -ivh hdf5-*

# lmdb
rpm -ivh lmdb-*

# openblas
rpm -ivh openblas-*
